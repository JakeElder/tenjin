{
  "private": true,
  "scripts": {
    "graduate": "npx lerna version --conventional-commits --conventional-prerelease --exact --ignore-changes '**/*.md'"
  },
  "workspaces": [
    "packages/*"
  ],
  "name": "workspace",
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
